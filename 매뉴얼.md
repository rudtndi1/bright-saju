# 좋은 기운 하루 — 블로그 자동화 매뉴얼

> 이 문서는 블로그 자동화 시스템 전체를 설명하는 한글 매뉴얼입니다.
> 작업 진행 상황은 `STATUS.md`를 참고하세요.

## 1. 시스템 개요

사주/운세 + 생활정보 블로그를 AI로 작성해 네이버 블로그에 자동 발행하는 시스템입니다.

```
소재 뱅크(855개 키워드) → Groq AI 글 생성 → Notion 저장
→ 이미지 확보(카드뉴스+실사) → Selenium 자동 발행 → 기록/로그
```

- **글 생성**: Groq API (`llama-3.3-70b-versatile`)
- **카테고리별 톤 차등**: 무료운세(전문+친근), 손없는날(실용정보), 작명(공감+전문), 이사(체크리스트), 선물(꿀팁)
- **이미지**: Pillow 카드뉴스 대표 + Unsplash 실사 본문 (로컬 파일 다운로드 → send_keys)
- **발행**: Selenium + 전용 Chrome 프로필 (`chrome_profile/`) + headed 모드
- **일정**: Windows 작업 스케줄러 매일 09:17 (정각 피함)
- **소재 뱅크**: 19개 세부 카테고리 ×45개 = 855개 롱테일 키워드 (중복 발행 방지)

## 2. 폴더 구조

| 파일 | 역할 |
|---|---|
| `main.py` | CLI 진입점 (명령어는 아래 §3) |
| `content_generator.py` | Groq 글 생성, 카테고리별 톤, AI 특유 표현 금지 |
| `topic_bank.py` | 소재 뱅크 관리 (미사용 키워드 선택, 사용완료 표시) |
| `topics.json` | 소재 데이터 (855개 롱테일 키워드) |
| `build_topic_bank.py` | 소재 뱅크 생성 스크립트 |
| `image_fetcher.py` | 이미지 확보 (카드뉴스 생성 + Unsplash/풀 실사 사진) |
| `notion_manager.py` | Notion 저장, 이미지 URL 반환, 상태 갱신 |
| `publish_approved.py` | Selenium 네이버 발행 (전용 프로필, 캡차 감지) |
| `login_profile.py` | **최초 1회** 전용 프로필 네이버 로그인 헬퍼 |
| `save_cookies.py` | 쿠키 저장 (세션 만료 시 재실행 가능) |
| `naver_auto_publish.py` | 구버전 발행기 (사용 안 함) |
| `run_daily.bat` | 작업 스케줄러 실행 진입점 (로그 기록) |
| `setup_task.ps1` | 매일 09:17 자동 작업 등록 스크립트 |
| `chrome_profile/` | 전용 Chrome 프로필 (자동 생성) |
| `images/` | 발행용 이미지 로컬 저장 |
| `fonts/` | 나눔고딕 폰트 (카드뉴스 이미지용) |
| `logs/` | 발행 로그 (날짜별) |
| `.env` | API 키·DB ID (절대 공개 금지) |

## 3. 명령어

모든 명령은 `C:\AI\bright-saju-blog` 폴더에서 실행합니다.

```bat
:: 소재 뱅크 현황 확인
venv\Scripts\python.exe main.py bank

:: 소재 뱅크 전체 초기화 (테스트용)
venv\Scripts\python.exe main.py bank-reset

:: 전체 카테고리/주제 목록
venv\Scripts\python.exe main.py list

:: 오늘 요일에 맞는 글 생성 → Notion 저장 (발행 안 함)
venv\Scripts\python.exe main.py daily

:: 특정 대분류에서 랜덤 주제 1개 생성 → Notion 저장
venv\Scripts\python.exe main.py generate 무료운세

:: 무인 전체 자동화: 소재선택 → 생성 → 이미지 → 발행 → 기록
venv\Scripts\python.exe main.py auto

:: N회차 연속 자동 발행 (스케줄러용)
venv\Scripts\python.exe main.py schedule 3

:: 드라이버 진단 테스트
venv\Scripts\python.exe main.py driver-test

:: 전체 19개 주제 한 번에 생성 (시간 오래 걸림)
venv\Scripts\python.exe main.py all
```

### 카테고리 구조 (요일별 자동 매핑)
| 요일 | 대분류 | 소주제 |
|---|---|---|
| 월 / 토 | 무료운세 | 오늘의 운세, 띠별 운세, 무료 궁합 |
| 화 / 일 | 손없는날_길일 | 손없는날, 이사 날짜, 개업 날짜, 결혼 택일 |
| 수 | 작명_출산 | 작명, 이름풀이, 출산 준비 |
| 목 | 이사_생활정보 | 이사 준비 체크리스트, 집들이 선물, 신혼집 준비, 자취 필수템, 생활용품 추천 |
| 금 | 선물_경조사 | 결혼선물, 집들이선물, 부모님선물, 명절선물 |

## 4. 최초 셋업 (1회만)

```powershell
cd C:\AI\bright-saju-blog

:: 1) 전용 프로필로 네이버 로그인
venv\Scripts\python.exe login_profile.py
# → Chrome 창이 열리면 네이버 로그인 → 로그인 후 Enter

:: 2) .env 파일에 API 키 확인
::    필수: GROQ_API_KEY, NOTION_TOKEN, NOTION_DATABASE_ID
::    선택: UNSPLASH_ACCESS_KEY (Unsplash 사진 검색), PEXELS_API_KEY (Pexels 검색)

:: 3) 드라이버 테스트
venv\Scripts\python.exe main.py driver-test

:: 4) 스케줄러 등록
powershell -ExecutionPolicy Bypass -File setup_task.ps1
```

## 5. 매일 09:17 자동 실행 설정

```powershell
:: 등록
powershell -ExecutionPolicy Bypass -File C:\AI\bright-saju-blog\setup_task.ps1

:: 즉시 1회 실행 테스트
schtasks /Run /TN BrightSajuBlogDaily

:: 등록 확인
schtasks /Query /TN BrightSajuBlogDaily

:: 로그 확인
Get-Content C:\AI\bright-saju-blog\logs\daily.log -Tail 50

:: 작업 삭제
Unregister-ScheduledTask -TaskName BrightSajuBlogDaily -Confirm:$false
```

## 6. Notion 상태 흐름

| 상태 | 의미 |
|---|---|
| `검토대기` | 생성되어 Notion에 저장됨 |
| `발행완료` | 네이버 발행 성공, `블로그URL` 채워짐 |
| `발행실패` | 발행 실패 (글은 Notion에 남아 있음) |

## 7. 문제 해결

### 🔴 로그인 실패 (네이버 세션 만료)
- 증상: 로그에 "로그인 실패 — 재로그인이 필요합니다"
- 해결: `venv\Scripts\python.exe login_profile.py` 실행 → 네이버 로그인

### 🔴 캡차 감지
- 증상: "캡차 감지됨 — 발행 중단"
- 원인: 동일 세션이 오래 사용됐거나 비정상 접근으로 의심됨
- 해결: 1~2시간 후 재시도 또는 `login_profile.py`로 재로그인

### Chrome 프로필 잠금 (이전 버전 이슈)
- 증상: Default 프로필 사용 시 Chrome 실행 중 충돌
- 해결: **전용 프로필(`chrome_profile/`) 사용으로 해결됨** (Chrome 실행 중에도 발행 가능)

### 이미지 삽입 실패
- 증상: "이미지 삽입 확인 실패"
- 해결: 자동으로 텍스트만 발행됨. 이미지 실패가 전체 발행을 막지 않음.

### 소재 뱅크 소진
- 증상: "모든 소재가 소진되었습니다"
- 해결: `main.py bank-reset`으로 초기화 또는 `build_topic_bank.py`로 재생성

## 8. 벤(저품질) 방지 가이드

이 시스템에 반영된 조치의 상세한 내용은 `벤방지_가이드.md`를 참고하세요.

**요약**:
- AI 특유 표현 50개+ 금지 (프롬프트에 명시)
- 불규칙 문장 길이, 개인 경험 에피소드 시작
- 소제목·표·체크리스트·FAQ 포함 (정보 밀도)
- 실사 사진 사용 (AI 생성 이미지 미사용)
- 정각 발행 회피 (09:17), 하루 1건
- 전용 프로필 + navigator.webdriver 숨기기

## 9. 보안 주의
- `.env`에 `GROQ_API_KEY`, `NOTION_TOKEN`, `NAVER_ID/PW`가 평문으로 저장되어 있다.
- 이 파일을 공유하거나 git에 커밋하지 말 것.
- `chrome_profile/` 폴더에 네이버 세션 정보가 저장되어 있으니 공개하지 말 것.
- `images/` 폴더의 이미지는 발행 후 정리해도 무방하다.
